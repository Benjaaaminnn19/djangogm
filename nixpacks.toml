[variables]
    # Estas variables "engañan" al sistema durante el build
    MYSQLDATABASE = "railway"
    MYSQLUSER = "root"
    MYSQLPASSWORD = ""
    MYSQLHOST = "127.0.0.1" 
    MYSQLPORT = "3306"

[phases.setup]
    aptPkgs = ["default-libmysqlclient-dev", "build-essential", "pkg-config"]